// The numeric constraint interface.
//
// Number is a lang item (LANGUAGE_SPEC.md section 6.1a) at the canonical
// path std::number::Number. The compiler satisfies it for the primitive
// numeric types (section 4.1); it bounds generic type parameters
// ('fn add<T: Number>(...)') and the 'to' conversion cast (section 4.5).

pub interface Number {
}
